package com.lms.tenantmanagement.config;

/** The scalar shape a {@link ConfigPropertyDefinition}'s value takes. No property uses {@code JSON} yet. */
public enum ConfigValueType {

	STRING, NUMBER, BOOLEAN, JSON

}
